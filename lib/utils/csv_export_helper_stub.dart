import 'dart:convert';

// Stub implementation for mobile platforms
class CsvExportHelper {
  static void exportCsvData(String csvData, String fileName) {
    // CSV export not available on mobile platforms
    throw UnsupportedError('CSV export is only available on web platform');
  }
}
